# Deployment Environment Configuration (.env.example)

# Client-side API key for GitHub Pages & production builds (optional, users can also input their key in the UI)
VITE_GEMINI_API_KEY=

# Server-side Gemini API key (for Node.js Express server environments)
GEMINI_API_KEY=
